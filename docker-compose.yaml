version: "3.3"

services:
  
  app:
    # image: node:14.17-alpine
    container_name: app_api
    build:
      context: .
      dockerfile: ./Dockerfile.dev
    ports:
      - 8000:8000
    volumes:
      - .:/app
    restart: always
    env_file: .env
    environment:
      NODE_ENV: development
      DATABASE_URL: postgres://admin:qwerty@db/games
      # sworking_dir: /app
    # command: sh -c "cd /api; npm run dev"
    networks:
      - main
    depends_on:
      - db      
  
  db:
    container_name: app_db
    hostname: db
    image: postgres:13.3-alpine
    ports:
      - 5432:5432
    volumes:
      # - ./postgres-initdb.sh:/docker-entrypoint-initdb.d/init.sh
      - db-data:/var/lib/postgresql/data/
    restart: always
    env_file: .env
    networks:
      - main

  # pgadmin:
  #   container_name: pgadmin
  #   hostname: pgadmin
  #   image: dpage/pgadmin4:latest
  #   restart: always
  #   env_file:
  #     - .env.dev
  #   ports:
  #     - 8080:80
  #   volumes:
  #     - pgadmin-data:/var/lib/pgadmin
  #   networks:
  #     - main
  #   depends_on:
  #     - db

volumes:
  # pgadmin-data:
  db-data:      
networks:
  main:
    driver: bridge   